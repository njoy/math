#ifndef MATH_API_INTERPOLATIONREGION_HPP
#define MATH_API_INTERPOLATIONREGION_HPP

#include "utility.hpp"

#include "math/API.hpp"
#include "math/interpolate.hpp"

class math::API::InterpolationRegion {
public:
  virtual ~InterpolationRegion(){}
  
  virtual double
  xMin() const = 0;
  
  virtual double
  xMax() const = 0;
  
  virtual std::vector< utility::View<double> >
  xGrid() = 0;
  
  virtual std::vector< utility::View<const double> >
  xGrid() const = 0;
  
  virtual std::vector< utility::View<double> >
  yGrid() = 0;
  
  virtual std::vector< utility::View<const double> >
  yGrid() const = 0;
  
  virtual double
  interpolate(const double x) const = 0;

  /*
  virtual std::unique_ptr< math::API::InterpolationRegionIntegral >
  integral(const double y0 = 0) const = 0;
  */
};

#endif
